# Dependencias
node_modules/

# Salida del build (la regenera Cloudflare en cada deploy)
dist/

# Secretos / entorno — el token de Airtable NUNCA se commitea
.env
.env.*
.dev.vars

# Config de desarrollo local (preview server, etc.)
.claude/

# Sistema operativo / editor
.DS_Store
Thumbs.db
*.log
.wrangler/
